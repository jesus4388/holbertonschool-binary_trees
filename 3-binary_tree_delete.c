#include "binary_trees.h"
/**
 */
void binary_tree_delete(binary_tree_t *tree)
{
    binary_tree_t *free = tree;
    binary_tree_t *aux = tree;

    if (tree != NULL)
    {
        while (free->left or free->aux)
        {
            free
        }
    }
}